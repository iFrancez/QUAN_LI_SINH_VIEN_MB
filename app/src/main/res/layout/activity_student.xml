<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="5dp"
    tools:context=".ActivityStudent">
    
    <androidx.appcompat.widget.Toolbar
        app:title="Sinh viên"
        app:titleTextColor="@color/blue"
        android:id="@+id/toolbarStudent"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize">

    </androidx.appcompat.widget.Toolbar>
    <EditText
        android:drawableEnd="@drawable/baseline_search_24"
        android:id="@+id/editTextSearchStudent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tìm kiếm mã số sinh viên" />
    <LinearLayout
        android:gravity="center"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="5dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:background="@drawable/background_button_2"
            android:layout_marginRight="60dp"
            android:textColor="#FFFFFF"
            android:id="@+id/buttonEnterSearchStudent"
            android:layout_width="145dp"
            android:layout_height="wrap_content"
            android:text="Tìm kiếm" />
        <Button
            android:background="@drawable/background_button_2"
            android:textColor="#FFFFFF"
            android:id="@+id/buttonRefreshStudent"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:text="Cập nhật lại" />
    </LinearLayout>
    <ListView
        android:divider="#FFFFFF"
        android:dividerHeight="5dp"
        android:id="@+id/listviewStudent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></ListView>

</LinearLayout>